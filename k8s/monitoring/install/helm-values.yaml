grafana:
  service:
    type: LoadBalancer

  adminPassword: admin

  dashboards:
    default:
      kubernetes:
        gnetId: 315
        datasource: Prometheus

prometheusOperator:
  admissionWebhooks:
    enabled: false
    patch:
      enabled: false

prometheus:
  prometheusSpec:
    serviceMonitorSelectorNilUsesHelmValues: false

alertmanager:
  enabled: false

crds:
  enabled: false